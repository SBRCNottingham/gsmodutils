language: python
python:
  - "2.7"
  - "3.5"
  - "3.6"
  - "3.7-dev"
# command to install dependencies
install:
  - sudo apt install -y docker
  - pip install -r requirements.txt scipy
# command to run tests
script:
  - py.test -n8 --cov=gsmodutils
after_success:
  - codecov